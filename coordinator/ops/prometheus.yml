global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "stelleth-coordinator"
    static_configs:
      # Point at the coordinator. When running outside Docker, use host.docker.internal.
      - targets: ["host.docker.internal:3000"]
    metrics_path: /metrics
